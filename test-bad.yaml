apiVersion: v1
kind: Pod
metadata:
  name: test-custom-bad
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:1.25-alpine
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 200m
        memory: 256Mi
